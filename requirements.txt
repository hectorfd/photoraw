# Dependencias de PhotoRAW.
# Las versiones son las probadas; el >= permite actualizar sin romper.

PySide6>=6.11          # la interfaz
qtawesome>=1.4         # iconos de la barra de herramientas

rawpy>=0.27            # decodificacion de RAW (LibRaw por debajo)
opencv-python>=4.10    # todo el trabajo de imagen del motor de revelado
numpy>=2.0
pillow>=11.0           # lectura de EXIF y de imagenes que no son RAW
pillow-heif>=1.0       # HEIC/HEIF: las fotos de iPhone y Android recientes

# Herramientas de IA. Con GPU NVIDIA (CUDA) instala 'onnxruntime-gpu'; si no
# tienes tarjeta compatible, cambia esta linea por 'onnxruntime' y todo
# funciona igual pero por procesador, mas despacio.
onnxruntime-gpu>=1.20
